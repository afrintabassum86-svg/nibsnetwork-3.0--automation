# ============================================
# AWS CONFIGURATION
# ============================================

# AWS Credentials (IAM User with S3 + RDS access)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=ap-south-1

# S3 Bucket (for storing images)
S3_BUCKET_NAME=nibsnetwork-images

# ============================================
# PostgreSQL (AWS RDS)
# ============================================
# Get these from AWS RDS Console after creating database
DATABASE_HOST=your-db-instance.xxxx.ap-south-1.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_NAME=nibsnetwork
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password

# Connection string format (alternative)
DATABASE_URL=postgresql://postgres:your_password@your-db-instance.xxxx.ap-south-1.rds.amazonaws.com:5432/nibsnetwork

# ============================================
# Frontend (Vite) - Public keys only
# ============================================
VITE_API_URL=http://localhost:3001
